# Liquid Glass — gum
#
# Shipped rather than generated, for the same reason as btop.theme: colors.toml
# is still jade because terminals and syntax need hue, and these prompts are
# chrome, not syntax.

env = FOREGROUND,##E0E0E0
env = BACKGROUND,##000000
env = BORDER_FOREGROUND,##FFFFFF
env = BORDER_BACKGROUND,##000000
